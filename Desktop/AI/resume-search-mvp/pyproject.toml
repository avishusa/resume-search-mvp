[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-search-mvp"
version = "0.1.0"
description = "FastAPI backend for a resume search MVP."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
